{
  "name": "@lepton/database",
  "version": "0.0.1",
  "private": true,
  "description": "Supabase migrations and database types",
  "files": [
    "dist"
  ],
  "typings": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "lint:fix": "eslint . --ext .ts --fix",
    "build:types": "npm_config_yes=true npx supabase gen types typescript --local > src/database.ts && pnpm lint:fix"
  },
  "devDependencies": {
    "typescript": "~5.0.2"
  }
}
