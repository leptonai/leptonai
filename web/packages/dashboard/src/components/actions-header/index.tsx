import { css } from "@emotion/react";
import { FC } from "react";

export const ActionsHeader: FC = () => {
  return (
    <div
      css={css`
        margin-left: 8px;
      `}
    >
      Actions
    </div>
  );
};
