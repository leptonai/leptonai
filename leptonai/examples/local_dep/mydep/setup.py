from setuptools import setup, find_packages

setup(
    name="mydep",
    version="0.1",
    description="mydep",
    author="mydep",
    author_email="mydep",
    url="mydep",
    packages=find_packages(),
    install_requires=[],
)
