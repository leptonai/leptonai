# Implementation note: mapping to lepton/api-server/httpapi/type_photon.go

# Implementation note: right now, since photons are mostly internal states,
# we do not explicitly expose the photon API objects to the user.
