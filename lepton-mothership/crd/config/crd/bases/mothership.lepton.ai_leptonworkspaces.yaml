---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.11.3
  creationTimestamp: null
  name: leptonworkspaces.mothership.lepton.ai
spec:
  group: mothership.lepton.ai
  names:
    kind: LeptonWorkspace
    listKind: LeptonWorkspaceList
    plural: leptonworkspaces
    shortNames:
    - lw
    singular: leptonworkspace
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: LeptonWorkspace is the Schema for the leptonworkspaces API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: LeptonWorkspaceSpec defines the desired state of LeptonWorkspace.
              The deployment environment inherits the one from cluster spec.
            properties:
              api_token:
                type: string
              cluster_name:
                type: string
              description:
                type: string
              enable_web:
                type: boolean
              git_ref:
                description: Terraform module git ref
                type: string
              image_tag:
                type: string
              name:
                description: Name is a globally unique name of a workspace within
                  mothership.
                type: string
              quota_cpu:
                type: integer
              quota_gpu:
                type: integer
              quota_group:
                type: string
              quota_memory_in_gi:
                type: integer
            required:
            - cluster_name
            - description
            - git_ref
            - name
            type: object
          status:
            description: LeptonWorkspaceStatus defines the observed state of LeptonWorkspace
            properties:
              last_state:
                description: Previously known workspace state.
                type: string
              state:
                description: Current workspace state.
                type: string
              updated_at:
                description: unix timestamp
                format: int64
                type: integer
            required:
            - state
            - updated_at
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
